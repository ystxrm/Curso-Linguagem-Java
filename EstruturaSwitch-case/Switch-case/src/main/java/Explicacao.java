void main() {
    /* Estrutura Switch-case
     * quando tem várias opções de fluxo a serem tratadas com base no valor de uma  variável
     * ao invés de várias estruturas if-else encadeadas, alguns preferem utilizar a estrutura
     * switch-case. */
}